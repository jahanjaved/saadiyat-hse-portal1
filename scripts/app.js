document.addEventListener('DOMContentLoaded', () => {
  const el = document.getElementById('app-status');
  if (el) {
    el.textContent = 'Step 1 package loaded successfully. Ready for SharePoint Excel connection in Step 2.';
  }
});
